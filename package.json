{
  "name": "projekt-aegis",
  "version": "0.2.3",
  "private": true,
  "description": "Projekt Aegis - Secure OpenClaw sidecar sandbox",
  "workspaces": [
    "apps/*",
    "packages/*",
    "extensions/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/aegis-desktop",
    "build": "npm run build --workspace packages/aegis-core && npm run build --workspace apps/aegis-controller && npm run build --workspace apps/aegis-desktop",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
